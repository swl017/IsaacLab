{
  "project": "iris_ma6",
  "last_updated": "2026-05-09",
  "last_session_ticket": "033-mappo-shared-optimizer-ownership",
  "features": [
    {
      "id": "controller",
      "name": "PX4-style cascaded drone controller",
      "status": "done",
      "spec": "doc/controller_spec.md",
      "notes": "Velocity→attitude→rate→motor cascade with gimbal and zoom control. mas/037: SIYI A8 zoom model (slew clip + dead-time buffer + integrator + first-order lag + 0.1-level publish quantization). 2026-05-07: zoom-controller bug fixes — ring buffer depth ceil(...)+1 with clamp [0, depth-1] (prevents d=depth wrap-around to 0 delay); per-call CUDA sync removed via _dead_time_all_zero Python bool cache refreshed in _refresh_dead_time_steps; env-side reset now mirrors controller._zoom.zoom (quantized published) into self.zoom_level so the first observation post-reset is consistent with subsequent steps."
    },
    {
      "id": "delay-system-v3",
      "name": "Multi-agent delay/staleness/dropout simulation",
      "status": "done",
      "spec": null,
      "notes": "Ringbuffer-based with timestamp guarantees. Fixed calling contract: advance/query split ensures idempotent multi-call per step. Stage order: staleness→latency→FOL→dropout. Ticket-015: continuous AoI — jittered capture timestamps propagated via explicit store() timestamp arg. Ticket-018: AgentStates.zero_() for buffer reuse, eliminates ~50 GPU allocs/step. Ticket-029: DelayPipelineV3 dual-cache advance(raw, noisy) with one shared delay realization; structurally fixes the shared-pipeline idempotency bug (reward/obs cached together on first advance). Bbox modeled as single field bboxes_2d with dual payload: raw=raycaster GT, noisy=detector-replicator output (mirrors raw when replicator disabled); one pipeline applies shared latency/staleness/dropout to both payloads. detection_pipeline_split flag removed. Regression test test_dual_cache.py locks in invariants; plot_dual_cache.py renders dual_cache_invariants.png. (2026-04-14)"
    },
    {
      "id": "cbf-safety",
      "name": "Two-layer collision avoidance (CPA + deploy filter)",
      "status": "done",
      "spec": "doc/safety_spec.md",
      "notes": "Training-time soft penalty + deployment-time hard filter"
    },
    {
      "id": "triangulation",
      "name": "Multi-camera multi-target triangulation with covariance",
      "status": "done",
      "spec": "doc/triangulation_spec.md",
      "notes": "Linear least-squares with condition number check"
    },
    {
      "id": "bbox-raycaster-v2",
      "name": "GPU-accelerated 2D bbox extraction with occlusion",
      "status": "done",
      "spec": "doc/bbox_spec.md",
      "notes": "Warp raycasting for occlusion detection. Detector replicator submodule added: Student's t noise with size-dependent scale, dual output (GT→rewards, replicated→obs). (2026-04-08)"
    },
    {
      "id": "detector-replicator",
      "name": "Calibrated detector noise + FP/FN replicator (YOLO sim-to-real)",
      "status": "done",
      "spec": null,
      "notes": "Ticket-009: Student's t noise. Ticket-010: FP/FN injection. Ticket-013 (closed): calibration refit — correlated w/h noise, dual sky/ground miss sigmoids, agent FP filtering (0.21→0.035), policy-driven calibration, background classification with ray direction check. Params baked into NoiseModelParams defaults. Manual tuning: 2x ground sigmoid, 0.1x sky, size cutoff floors. (2026-04-09)"
    },
    {
      "id": "initial-states",
      "name": "Curriculum-driven initial state randomization",
      "status": "done",
      "spec": "doc/initial_states_spec.md",
      "notes": "Cylinder-based placement with curriculum height/orientation/zoom. Tuned for ma5-parity at progress=0 (2026-03-20)"
    },
    {
      "id": "domain-randomization",
      "name": "Sim-to-real domain randomization",
      "status": "done",
      "spec": "doc/domain_randomization_spec.md",
      "notes": "Camera FOV, physics, gimbal randomization"
    },
    {
      "id": "target-controller",
      "name": "Target movement with behavior FSM",
      "status": "done",
      "spec": "doc/target_movement_spec.md",
      "notes": "Approach/evade/linear/circular modes using DroneController"
    },
    {
      "id": "visualization",
      "name": "Debug visualization (frustums, ellipsoids, detection)",
      "status": "done",
      "spec": "doc/visualization_spec.md",
      "notes": "Camera frustums, LOS indicators, covariance ellipsoids"
    },
    {
      "id": "curriculum",
      "name": "Curriculum learning system",
      "status": "in_progress",
      "spec": "doc/curriculum_spec.md",
      "notes": "Redesigned: decoupled agent velocity ramp (0-40k, 3→10m/s) from target motion (40k-120k). Observability phases pushed after target mastery (noise 120k, delay 140k+). Added FP/FN phase (fp_fn_start/end_step, default co-located with noise 100k-120k). Anti-forgetting: tau_zoom/max_lin_vel wide-range randomization. Backlog: per-agent noise/dropout/max_lin_vel (2026-04-08)"
    },
    {
      "id": "env-integration",
      "name": "Full environment integration and training",
      "status": "in_progress",
      "spec": "doc/iris_ma6_env_spec.md",
      "notes": "Obs: 31D ego (pos, vel, rpy, ang_vel_b, lin_acc_b, gimbal joints, ray_dir_w, combined_ang_vel_w, bbox_aoi, zoom, effective_hfov, bbox) + 16D*(N-1) inter-agent [+6D tri]. Ticket-012: added effective HFOV (1D, radians) to ego obs — physical quantity, deployment-ready from camera calibration (2026-04-09). Ticket-030: registered heading-frame v1 env as separate gym task (`Isaac-Iris-MA6-Direct-V1-v0`) and wired experiment/eval task selection so short A/B validation can target baseline vs v1 without code edits. (2026-04-15). 2026-05-07: asymmetric actor-critic — `cfg.enable_critic_continuous_zoom` (default False) auto-sets `state_space = obs_dim*N + 2*N` and the env's `_get_states()` returns concat(actor_obs) ++ [zoom_internal, zoom_target] per agent. Actor's per-agent obs unchanged (still sees quantized published zoom that hardware exposes); centralized MAPPO critic gets the privileged continuous zoom state via skrl's existing `env.state()` → infos plumbing in `multi_agent_train`. Motivation: with quantum=0.1 and max Δzoom_per_step≈0.08, the symmetric critic blurs V predictions across all internal states sharing a quantum bin; the asymmetric critic resolves them, sharpening V and temporal credit assignment for the integrator chain that produces a quantum crossing several steps after the action. Toggle is the next training experiment to run against the failing siyi_a8 + sum8/delta12 config that earlier collapsed."
    },
    {
      "id": "experiments",
      "name": "Experiment registry, training runners, and evaluation pipeline",
      "status": "done",
      "spec": "doc/experiments_spec.md",
      "notes": "42 experiments (A1/A3/A4/A5/A6/A7/baselines/sweeps), 4 suites, metric tracker with CBF metrics, greedy baseline with gimbal tracking. Ported from iris_ma5, dropped MLP path. Ticket-030: ExperimentCfg now carries a `task` field; run/eval resolve task precedence as CLI `--task` > experiment task > default baseline task. Added `validation_obs_v1_short` for short heading-frame A/B validation. (2026-04-15). Ticket-033 (2026-05-09): MAPPO-RNN trainer fix — optimizer/scheduler ownership keyed by parameter-tensor identity (`mappo_rnn_groups.py` + `mappo_rnn.py` _update refactor). Homogeneous shared models (current iris_ma6) collapse to one Adam + one KLAdaptiveLR with aggregated losses; heterogeneous setups fall through to per-uid layout unchanged. Effective LR is now ~halved per shared group (one step per minibatch instead of two sequential per-uid steps); existing learning_rate values may need ~2x bump or longer training to match early-phase progress of pre-fix runs. 20 tests in scripts/reinforcement_learning/skrl/tests/ (groups/init/update). Pending real-run validation."
    },
    {
      "id": "controller-tuning",
      "name": "Oscillation-aware parallel controller auto-tuning",
      "status": "done",
      "spec": null,
      "notes": "Real DroneController in tuner, oscillation metrics (damping/ZC/amplitude/freq), multi-speed tests (5+10 m/s at 100Hz), step response PDF plotter, TuningScoreWeights, gain coupling in search, --aero-level flag. Spawned ticket-004 (drag FF) and ticket-006 (DR integration). (2026-04-06)"
    },
    {
      "id": "sysid-replicator",
      "name": "PX4 SITL response matching via gain sweep",
      "status": "done",
      "spec": null,
      "notes": "sysid_replicator.py: loads PX4 SITL CSVs, resamples 125→100 Hz, scores DroneController gain candidates via timeseries MSE (vel 60%/att 25%/rate 15%). 4 tests (hover, vel_5, impulse, yaw). 3-way PDF overlay (PX4-matched vs PX4 SITL vs current). Exports PX4_MATCHED_CONTROLLER_CFG. vel_5 settling matched to 0.7%. (2026-04-06)"
    },
    {
      "id": "domain-randomization-integration",
      "name": "DomainRandomizer wiring into env _reset_idx",
      "status": "done",
      "spec": null,
      "notes": "Integrated: DomainRandomizer instantiated in __init__, randomize_all() called in _reset_idx, curriculum-gated by progress_dynamics (180k-200k). Camera FOV→intrinsic scale flows to bbox raycaster + delay system. Physics mass/inertia written to sim assets (controller keeps nominal mass for model mismatch). Gimbal offsets added to joint position targets. Gimbal dynamics (stiffness/damping) applied to actuators. Mount offset enabled. (2026-04-06)"
    }
  ]
}
